# Bash configuration

Bash needs the following to run smooth:

#+begin_src
  pacman -S fzf direnv starship eza bat ripgrep
#+end_src

- fzf:      Fast history search
- direnv:   Globals that work well with everything, incl Emacs
- starship: Better prompt
- eza:      Nicer ls
- bat:      Better cat
- ripgrep:  Faster grep

Put the following in ~/.bashrc

#+begin_src shell

  [[ $- != *i* ]] && return

  # --- FZF integration ---
  if [ -f /usr/share/fzf/key-bindings.bash ]; then
    source /usr/share/fzf/key-bindings.bash
  fi

  if [ -f /usr/share/fzf/completion.bash ]; then
    source /usr/share/fzf/completion.bash
  fi

  eval "$(direnv hook bash)"
  eval "$(starship init bash)"

  alias ls="eza --icons"
  alias ll="eza -lah --git"
  alias tree="eza --tree"

  alias grep='grep --color=auto'
  alias l='eza -lah --group-directories-first --icons $1'

  alias cat="bat"

  function cd() {
      builtin cd "$@" && eza -lah  --group-directories-first --icons
  }

  alias ..="cd .."
  alias ...="cd ../.."
  alias cls="clear"

#+end_src

For Starship, use

#+begin_src toml
  add_newline = false

  format = """
  $directory\
  $git_branch\
  $git_status\
  $fill\
  $cmd_duration\
  $line_break\
  $character"""

  [fill]
  symbol = " "

  [directory]
  style = "bold blue"
  truncate_to_repo = true
  truncation_length = 3
  read_only = " ro"

  [git_branch]
  symbol = " "
  style = "bold purple"

  [git_status]
  style = "red"
  format = '[$all_status$ahead_behind]($style) '

  [cmd_duration]
  min_time = 500
  format = '[$duration]($style) '
  style = "yellow"

  [character]
  success_symbol = "[❯](bold green)"
  error_symbol = "[❯](bold red)"
  vimcmd_symbol = "[❮](bold green)"

  [java]
  format = '[☕ $version](red) '
  detect_extensions = ["clj", "cljs", "cljc", "java", "edn"]
  detect_files = ["deps.edn", "project.clj", "build.clj"]
  detect_folders = []

  [nodejs]
  disabled = true

  [python]
  disabled = true

  [golang]
  disabled = true

  [rust]
  disabled = true

  [php]
  disabled = true

  [ruby]
  disabled = true

  [package]
  disabled = true

  [aws]
  disabled = true

  [gcloud]
  disabled = true

  [docker_context]
  disabled = true

  [kubernetes]
  disabled = true

  [hostname]
  disabled = true

  [username]
  disabled = true

  [memory_usage]
  disabled = true

  [battery]
  disabled = true

  [time]
  disabled = true
#+end_src
